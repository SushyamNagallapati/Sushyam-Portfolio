

## Add Google Analytics Tag

Add the Google Analytics (gtag.js) snippet to `index.html` inside the `<head>` tag.

### File: `index.html`
- Insert the two `<script>` tags (async loader + config) at the top of `<head>`, before the existing `<meta charset>` tag.

